export default defineAppConfig({
  ui: {
    colors: {
      neutral: 'neutral',
    },
    button: {
      slots: {
        base: 'rounded-lg flex items-center justify-center cursor-pointer',
      },
    },

  },
})
